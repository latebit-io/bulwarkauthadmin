services:
  mongodb:
    image: mongo:7.0.0
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: bulwarkauth
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
